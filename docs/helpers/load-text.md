---
layout: "docs_with_sidebar"
title: "load_text()"
permalink: "/docs/helpers/load-text/"
autogenerated_api_symbol: true
api_category_permalink: "/docs/helpers/"
---

# `load_text()`

Part of **Helper Utilities** (`qhchina.helpers.texts.load_text`).

[View source](https://github.com/mcjkurz/qhchina/blob/main/qhchina/helpers/texts.py#L235)

<pre class="signature"><code><span class="sig-name">load_text</span>(<span class="sig-param">filename</span>, <span class="sig-param">encoding</span> <span class="sig-punct">=</span> <span class="sig-default">'utf-8'</span>)</code></pre>

Loads text from a file.

**Parameters:**
- `filename` (str): The filename to load text from.
- `encoding` (str): The encoding of the file. Default is "utf-8".
  Use "auto" to automatically detect the encoding.

**Returns:**
(str) The text content of the file.


**Example:**
```python
from qhchina.helpers.texts import load_text

result = load_text(filename=...)
```
